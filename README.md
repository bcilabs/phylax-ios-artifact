# phylax-ios-artifact
A dedicated repository for distributing the prebuilt Phylax iOS SDK XCFramework binaries. It hosts versioned .xcframework packages—automatically updated via CI/CD on each merge—so consumers can grab the compiled SDK without having to build it from source.
